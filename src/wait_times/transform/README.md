# Harmonization

Transformation code maps source labels to canonical province/procedure codes and produces the schema in `docs/data_dictionary.md`. Assign a comparability tier before records are exposed to national dashboard views.
