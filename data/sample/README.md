# Synthetic sample data

`wait_times_example.csv` contains invented values solely for testing the schema, SQL views and developer setup. Do not cite, chart or interpret these values as Canadian health data.
