# Dashboards

- `powerbi/`: `.pbix` files stay local; commit screenshots, a data model note and measures as text.
- `tableau/`: extracts and packaged workbooks stay local.
- `streamlit/`: optional reproducible Python dashboard.

Every page should show the data release, reporting period and comparability note.
